--love.load
player = {
    x = lg.getWidth() / 2,
    y = lg.getHeight() / 2,
    angle = 0,
    rotateSpeed = 4,
    speed = 320,
    speedX = 0,
    speedY = 0,
    sprite = lg.newImage('sprites/pudding1.png')
}

return player
